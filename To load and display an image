display a picture in collab:-

import cv2
import numpy as np
from matplotlib import pyplot as plt

img = cv2.imread("minion.jpg")#add a photo from your gallery and change the name accordingly wrt png,jpg
img_cvt=cv2.cvtColor(img, cv2.COLOR_BGR2RGB)
plt.imshow(img_cvt)
plt.show()
